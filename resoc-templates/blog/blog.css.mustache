@import url('https://fonts.googleapis.com/css2?family=Bree+Serif&family=Fira+Code&display=swap');

.wrapper {
  display: flex;
  flex-direction: column;
}

main {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  position: relative;
  margin: 2vh 3vw 9vh 20vw;
}

h1 {
  text-align: right;
  background: rgb(11,35,238);
  background: linear-gradient(72deg, #e23500, #ffbc2d);
  -webkit-text-fill-color: transparent;
  -webkit-background-clip: text;
  font-family: 'Bree Serif', serif;
  font-size: 16vh;
  text-transform: uppercase;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
}

h2 {
  color: white;
  margin: 0;
  font-family: 'Fira Code', monospace;
  font-size: 10vh;
}

footer {
  flex: 0 0;
  min-height: 20vh;
  display: flex;
  align-items: center;
  background: rgb(11,35,238);
  background: linear-gradient(72deg, #ffbc2d, #e23500);
  padding: 2vh 3vw 2vh 3vw;
}
